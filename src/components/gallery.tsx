'use client';
import Image from 'next/image';
import {useEffect,useRef,useState} from 'react';
import {X} from 'lucide-react';
type GalleryItem={id:string;title:string;image:string|null};
export function Gallery({items}:{items:GalleryItem[]}){const [active,setActive]=useState<GalleryItem|null>(null);const closeRef=useRef<HTMLButtonElement>(null);const trigger=useRef<HTMLElement|null>(null);useEffect(()=>{if(!active)return;trigger.current=document.activeElement as HTMLElement;closeRef.current?.focus();const old=document.body.style.overflow;document.body.style.overflow='hidden';const key=(event:KeyboardEvent)=>{if(event.key==='Escape')setActive(null);if(event.key==='Tab'){event.preventDefault();closeRef.current?.focus()}};window.addEventListener('keydown',key);return()=>{document.body.style.overflow=old;window.removeEventListener('keydown',key);trigger.current?.focus()}},[active]);return <><div className="gallery-grid">{items.filter(i=>i.image).map(i=><button className="gallery-tile" key={i.id} onClick={()=>setActive(i)} aria-label={`Enlarge ${i.title}`}><Image src={i.image!} alt={i.title} fill sizes="(max-width:700px) 90vw, 33vw"/><span>{i.title}</span></button>)}</div>{active&&<div className="lightbox" role="dialog" aria-modal="true" aria-label={active.title} onClick={()=>setActive(null)}><button ref={closeRef} onClick={()=>setActive(null)} aria-label="Close image"><X size={30}/></button><Image src={active.image!} alt={active.title} width={1400} height={1000} onClick={e=>e.stopPropagation()}/></div>}</>}
export function youtubeId(url:string){try{const u=new URL(url);if(!['youtube.com','www.youtube.com','youtu.be'].includes(u.hostname))return null;const id=u.hostname==='youtu.be'?u.pathname.slice(1):u.searchParams.get('v')||u.pathname.split('/').at(-1);return id&&/^[A-Za-z0-9_-]{11}$/.test(id)?id:null}catch{return null}}
